/**
 * Created by Jackey Li on 2016/2/5.
 */
(function () {
    'use strict';

    var methods = ['set', 'get'];

    methods.forEach(function (method) {

    });

    //one param: string
    //multi param
    //function
    //array
    function create() {
        if (arguments.length == 1) {
            //if(){}
        }
    }
})();